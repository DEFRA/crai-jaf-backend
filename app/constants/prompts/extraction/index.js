const { deliverablesPrompt } = require('./deliverables')
const { detailsPrompt } = require('./details')
const { jobSummaryPrompt } = require('./job-summary')
const { keyResponsibilitiesPrompt } = require('./key-responsibilities')
const { knowledgePrompt } = require('./knowledge')
const { mainActivitiesPrompt } = require('./main-activities')
const { skillsPrompt } = require('./skills')

module.exports = {
  deliverablesPrompt,
  detailsPrompt,
  jobSummaryPrompt,
  keyResponsibilitiesPrompt,
  knowledgePrompt,
  mainActivitiesPrompt,
  skillsPrompt
}
