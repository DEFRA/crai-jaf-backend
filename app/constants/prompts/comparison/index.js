const { competencyPrompt } = require('./competency')
const { linkagePrompt } = require('./linkage')
const { overallPrompt } = require('./overall')
const { taskPrompt } = require('./task')

module.exports = {
  overallPrompt,
  taskPrompt,
  competencyPrompt,
  linkagePrompt
}
